# dbconnector/dbconnector.py
## вопросы
1) Почему используется `odbc` вместо других DBAPI?
2) Почему список драйверов составляется рандомно через статический метод?
```Python
@staticmethod
def get_odbc_drivers() -> List[str]:
	return [i for i in pyodbc.drivers() if i.startswith('ODBC')]
```
3) А потом из этого списка вытаскивается первый драйвер по индексу?
```Python
def connect(self):
	driver = self.get_odbc_drivers()[0]
```
uat_depers01
message_id kafka_message_history guid посмотреть библиотеку на питоне для генерации гуида
# correctionIn error
## check.py
Две функции тестирования `test_check_correct_positions()` и `test_check_positions` передают в `check()` аргументы:
1) `query_positionRUS`, `query_positionUSD`
2) `'server'`
`query_positionRUS` и `query_positionUSD` - это имена блоков в ini файле `query_settings.ini`. Внутри каждого из блоков лежит переменная `positionRUS`/`positionUSD` - обе переменные хранят пути до SQL файлов, где описаны команды выбора этих данных из БД.
Функция `check()` принимает позиционные аргументы:
```Python
def check(query, data_query, body, message, position, server):
	pass
```
Ошибка возникает на следующем шаге - при передаче `query` и `server` в `db_query`:
```Python
def check(query, data_query, body, message, position, server):
	dataset = dbconnector.db_query(server, query)
```
В `query` и `server` значения:
1) в `query` лежит строка:
```Python
f'query_position{currency}'
```
2) в `server` лежит строка:
```Python
'server'
```
В `currency` попеременно лежат два значения `RUB` или `USD`.
## dbconnector.py
Тут вся работа происходит в функции `db_query`.
Сначала мы достаем конфиг:
```Python
config = get.get_config('query_settings.ini')
```
Из конфига достаем все нужные файлы для коннекта с БД.
После коннекта используем параметр, который был передан, как `query` из функции `check()` в функцию `db_query()`:
```Python
def check(query, data_query, body, message, position, server):
	dataset = dbconnector.db_query(server, query)
...
def db_query(server, param):
	config = get.get_config('query_settings.ini')
	...
	queries = get.get_config_keys(config, param)
```
Функция `get_config_keys()`нужна, чтобы из нужной части конфига достать все ее ключи и значения. Часть конфига, из которой мы будем доставать значения передается в аргументе `param`, а сам конфиг в`config`.
В данном случае наш `param` = `query` = `query_positionRUS\query_positionUSD`. То есть из конфига нам нужна секция `query_positionsRUS\query_positionsUSD` в двух разных случаях.
На выходе должен получится словарь, который содержит ключи и их значения из ini файла конфигураций. В качестве значений будут даны пути до SQL файлов, где прописаны `SELECT` запросы для выборки нужных данных.
Далее функция `insert_pararms()` поменяет в словаре пути до SQL файлов на те SQL команды, которые лежат внутри них.
## результат
Ошибка состояла в том, что внутри БД два сообщения с одинаковыми `MessageId` перезаписывали друг друга.

# как найти?
## как найти клиента в BQort?
Здесь указаны пути по интерфейсу `BQort`:
-  `Справочники` -> `Деловые партнеры` -> `Фильтр ролей ДП (надо выбрать "Клиент"`) -> `Фильтр по "Код в БО"`.
- `Справочники` -> `Деловые партнеры` -> `Фильтр по "Код в БО"`.
У клиента еще есть разные интересные параметры, например, счета клиента. Чтобы их найти:
- `ПКМ` по конкретному клиенту.
- `Счета` -> `Субсчета и аналитические счета`/`Физические и синтетические счета` (что такое физический и синтетический счет?).
Дополнительные вещи (меня интересовали "Счета ДЕПО") можно найти так:
- `ПКМ` по конкретному клиенту.
- `Данные деловых партнеров` -> `Счета ДЕПО`.
## как найти сделку в BQort?
Здесь указаны пути по интерфейсу `BQort`:
- `Операции` -> `Сделки`
Тут можно поискать сделку по `ID` (`ID сделки` - это `id` в таблице `DB.Trades`) или по номеру сделки (`Сделки с номерами` - это `TradeNum` в таблице `DB.Trades`)
У сделки можно посмотреть этапы. Для этого нужно сначала найти сделку, а потом:
- `ПКМ` по сделке.
- `Данные по сделкам` -> `Этапы сделки`
# как создать?
# как создать сделку?
Чтобы создать сделку нужно действовать через `TDB.`

# вопросы
- [x] Что такое `&BOCode`?
- [x] Как авторизироваться в сваггере?
- [x] Как заполнить Dev Complite?
- [ ] Cверки с рынками (валютный, фондовый, срочный)? Сверка с биржевым отчетом?
- [ ] Операционный день?
	- [ ] Операционный день обновляется в 4 утра.
- [ ] Что такое счет ДЕПО? 
	- [ ] Понятно, что это связно с депозитарием.
	- [ ] Счет ценных бумаг в депозитарии для аккаунта клиента?
- [ ] Комиссии?
	- [ ] Физические лица (тариф "Родной").
	- [ ] Индивидуальный условия по взыманию комиссий.
	- [ ] Разные тарифные планы (куча тарифных планов).
	- [ ] Только после того как прошла оплата по сделке расчитывается комиссия (ночью в 2 часа).
## QUIK
Это система, которая живет в течении дня. На следующий день все сделки очищаются - считается, что они закрыты.