TARGET DECK: linuxDeck

Где можно посмотреть список текущих пользователей? #flashcard 
В файле `passwd`. Он лежит в директории `etc`. Путь: `/etc/passwd`.
<!--ID: 1747496718728-->


Где можно посмотреть список текущих групп? #flashcard 
В файле `group`. Он лежит в директории `etc`. Путь: `/etc/group`.
<!--ID: 1747496718731-->


Расшифруй запись: `peterpetukhov:x:1000:1000:peterpetukhov,,,:/home/peterpetukhov:/usr/bin/zsh` #flashcard 
1) `peterpetukhov` - имя пользователя.
2) `x` - это место для пароля пользователя, но он скрыт в специальном файле.
3) `1000` - идентификатор пользователя.
4) `1000` - идентификатор группы пользователя. 
5) `peterpetukhov,,,` - дополнительная информация (телефон, почта и т.д.)
6) `/home/peterpetukhov` - домашняя директория пользователя.
7) `/usr/bin/zsh` - оболочка, которая будет запускаться при заходе пользователя в систему.
<!--ID: 1747496907397-->

Как можно узнать имя текущего пользователя? #flashcard 
```Bash
whoami
или
echo $USER
```
<!--ID: 1748174367950-->


Как посмотреть список групп, в которых состоит пользователь? #flashcard 
```Bash
groups <пользователь>
```
<!--ID: 1748174367952-->


Как создать нового пользователя? #flashcard 
```Bash
sudo adduser <имя пользователя>
```
<!--ID: 1748174367953-->


Что такое и где лежит файл `sudoers`? #flashcard
`sudoers` помимо прочего содержит пользователей, которые могут использовать команду `sudo` для выполнения операций. В него можно дописать новых пользователей. Путь: `/etc/sudoers`.
<!--ID: 1748174367954-->


Как поменять пользователя? #flashcard 
```Bash
su <пользователь> 
```
<!--ID: 1748174367955-->


Как создать группу? #flashcard 
```Bash
sudo groupadd <имя группы>
```
<!--ID: 1748174367956-->


Как добавить пользователей в группу? #flashcard 
```Bash
sudo usermod -aG <группа> <пользователь>
или
sudo gpasswd -a <пользователь> <группа>
```
<!--ID: 1748174367957-->


Как удалить из группы? #flashcard 
Можно переопределить список пользовательских групп:
```Bash
sudo usermod -G <список, через запятую> <пользователь>
```
А можно просто удалить:
```Bash
sudo gpasswd -d <пользователь> <группа>
```
<!--ID: 1748174367958-->


Как удалить группу? #flashcard 
```Bash
sudo delgroup <группа>
```
Но здесь могут возникнуть сложности - группа может оказаться главной для пользователя (единственной, в которой он состоит). Чтобы все равно удалить даже такую группу:
```Bash
sudo groupdel -f <группа>
```
<!--ID: 1748174367959-->


Как удалить пользователя? #flashcard 
```Bash
sudo deluser <пользователь>
```
<!--ID: 1748174367960-->


Как удалить пользователя и все файлы, создателем которых он является? #flashcard 
```Bash
sudo deluser --remove-all-files <пользователь>
```
<!--ID: 1748174367961-->


Как удалить пользователя и его домашнюю директорию? #flashcard 
```Bash
sudo deluser --remove-home <пользователь>
```
<!--ID: 1748174367962-->


Как сменить пароль пользователю? #flashcard 
```Bash
sudo passwd <пользователь>
```
<!--ID: 1748174367963-->


Как сделать бэкап при удалении пользователя? #flashcard 
```Bash
sudo deluser --backup-to <путь> <пользователь>
```
<!--ID: 1748174367964-->


Расшифруй запись: `-rw-r--r-- 1 peterpetukhov peterpetukhov 12 May 17 11:51 hello` #flashcard 
1) `-rw-r--r--` - это режимы доступа к файлу. Первый символ - указывает на то, что файл является файлом, три символа после (`rw-`) указывают на то, что может делать с файлом владелец, еще три следующих определяют, что с файлом может делать группа, которая им владеет (`r--`) и последние три - что с файлом могут делать все остальные (`r--`).
2) `1` - номер файла в папке.
3) `peterpetukhov` - имя владельца.
4) `peterpetukhov` - имя группы.
5) `12 May 17 11:51` - дата и время создания файла.
6) `hello` - имя файла.
<!--ID: 1748174367965-->


Как изменить права доступа к файлу? #flashcard 
```Bash
sudo chmod <для кого меняем(u,g,o,a)><+/-><группа прав (r,w,x)> <файл>
```
<!--ID: 1748174367966-->


Какие есть группы прав у файлов? #flashcard 
1) `r` - право на чтение файла.
2) `w` - право на редактирование файла.
3) `x` - право на запуск файла.
<!--ID: 1748174367967-->

Как скачать файл или интернет страницу по ссылке в Linux? #flashcard 
С помощью утилиты `wget`. Базово на вход ей передается ссылка и она скачивает любое ее содержимое.
```Bash
wget <ссылка>
```

Что будет если передать `wget` ссылку на обычную интернет страницу? #flashcard 
Она скачает HTML документ по ней в ту директорию, из которой была вызвана.

Как разархивировать архив в Linux? #flashcard 
С помощью утилиты `tar` и ее дополнительных параметров:
1) `-x` - (extract) извлечение.
2) `-z` - (gzip) это движок распаковки (может быть разным).
3) `-v` - (verbose) будет писать в терминал отчет по распаковке.
4) `-f` - (file) после этого пишется имя файла, который мы будем распаковывать.
```Bash
tar -xzvf <имя архива>
```

Как заархивировать файлы в Linux? #flashcard 
С помощью утилиты `tar` и ее дополнительных параметров:
1) `-c` - (create) создание.
2) `-f` - (file) после этого аргумента пишется имя файла, который мы будем распаковывать.
```Bash
tar -cf <имя будущего архива> <файл\папка>
```

Как изменить владельца папки, всех ее файлов и поддиректорий? #flashcard 
Нужно применить команду `chown` + указать , что мы будем делать смену владельца рекурсивно с помощью параметра `-R`:
```Bash
chown -R <новый владелец> <директория>
```

Как добавить переменную окружения? #flashcard 
Можно 1) добавить в рамках текущей сессии и 2) для всех прочих сессий, которые открыты после текущей:
```Bash
# добавляем в рамках текущей сессии
export PGDATA=/var/lib/postgresql/16/main
# добавляем в рамках всех прочих сессий
echo "export PGDATA=/var/lib/postgresql/16/main" >> ~/.zshrc
```

Как добавить новый путь в PATH? #flashcard 
Можно сделать это через `.zshrc`. Можно 1) добавить в рамках текущей сессии и 2) для всех прочих сессий, которые открыты после текущей:
```Bash
# добавляем в рамках текущей сессии
export PATH="$PATH:/var/liv/postgresql/16/bin"
# добавляем в рамках всех прочих сессий
echo 'export PATH="$PATH:/var/lib/postgresql/16/bin"' >> ~/.zshrc
```
<!--ID: 1750151998562-->


Какие шаги нужно предпринять, чтобы установить PostgreSQL из исходников? #flashcard 
Нужно:
1) скачать архив с официального сайта с помощью `wget`.
2) распаковать архив с помощью утилиты `tar`.
3) зайти в архив и провести конфигурацию файлов:
```Bash
./configure --prefix=<имя папки куда будем устанавливать>
```
4) провести установку с помощью утилиты `make`:
```Bash
make world
sudo make install-world
```
5) присвоить себе права над новой директорией с помощью утилиты `chown`
6) теперь нужно сделать новую переменную окружения `PGDATA` (она нужна, чтобы указывать на место хранения будущей конфигурации кластера):
```Bash
echo "export PGDATA=/var/lib/postgresql/16/main" >> ~/.zshrc
```
7) инициализируем первый кластер с помощью утилиты `pg_ctl`:
```Bash
/var/lib/postgresql/16/bin/pg_ctl initdb
```
8) запускаем сервер:
```Bash
/var/lib/postgresql/16/bin/pg_ctl -D /var/lib/postgresql/16/main start
```
9) вносим путь до `psql` в переменные окружения:
```Bash
export 'PATH="$PATH:/var/lib/postgresql/16/bin"'
```
<!--ID: 1750151998568-->


Я положил в мою таблицу PostgreSQL слово `hello`. Таблица находится в tablespace по адресу `/mnt/pg_tablespaces/ts_data`. Как мне найти в файлах БД свое слово? #flashcard 
Я могу прочитать файлы, которые хранятся в табличном пространстве с помощью команды `xxd` - она показывает содержимое файла в бинарном формате и если там будут буквы в формате ASCII.
<!--ID: 1750151998570-->

Как проверить версию Debian? #flashcard 
```Bash
cat /etc/os-release
```
<!--ID: 1750250106733-->

Как выйти из утилиты `sqlite`? #flashcard 
```SQL
.exit
```
<!--ID: 1750527557202-->
