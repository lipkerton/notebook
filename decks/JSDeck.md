TARGET DECK: JSDeck

Какие есть среды для написания JS кода? #flashcard 
Node.js и консоль браузера.
<!--ID: 1748174367968-->


Какие есть основные характеристики у языка JS? #flashcard 
Он интерпретируемый, динамический и высокоуровневый.
<!--ID: 1748174367969-->


Как создать переменную в JS? #flashcard 
Чтобы создать переменную ее сначала нужно объявить с типом `let` и только потом уже присвоить ей значение:
```JavaScript
let a, b;
a = 0;
b = 1.01;
```
<!--ID: 1748174367970-->


Что такое объект в JS? #flashcard 
Это конструкция, которая похожа на словарь в Python - фигурные скобки содержат пары `имя: значение`. Такие пары называются свойствами:
```JavaScript
let a;
a = {title: "book_title"};
```
<!--ID: 1748174367971-->


Как получить доступ к свойствам объекта в JS? #flashcard 
К свойствам можно получить доступ с помощью точки или квадратных скобок:
```JavaScript
let a;
a = {title: "book_title"};
a.title  // => "book_title".
a["title"]  // => "book_title".
```
<!--ID: 1748174367972-->


Что такое оператор в JS? #flashcard 
Оператор - это то, что используется для вычисления значения, например, знак присваивания, знак сложения, знак вычитания.
<!--ID: 1748421292006-->


Что такое управляющая структура в JS? #flashcard 
Управляющие структуры - это подвид операторов в JS. Под управляющими структурами мы имеем в виду циклы, условия и проч.
<!--ID: 1748421292007-->


Как сделать массив в JS? #flashcard 
Массивы в JS - это буквально списки из Python с небольшим исключением. Массив создается так:
```JavaScript
let x = [1, 2, 3, 4];
```
<!--ID: 1748421292008-->

Какие свойства у массива в JS? #flashcard 
Внутри массива можно хранить любые значения, в т.ч. значения разных типов. Доступ к элементам массива можно получить по индексам, по индексам же можно изменить элементы массива и добавить новые. Однако срезы недоступны в JS, точно так же как недоступен и доступ к элементам через отрицательный индекс.
<!--ID: 1767949581431-->


Как измерить длину массива в JS? #flashcard 
С помощью атрибута `length`:
```JavaScript
let x = [1, 2, 3, 4];
x.length
```
<!--ID: 1748421292009-->


Как получить последний элемент массива в JS? #flashcard 
С помощью атрибута `length`:
```JavaScript
let x = [1, 2, 3, 4];
x[x.length - 1]
```
<!--ID: 1748421292010-->


Какие математические операторы есть в JS? #flashcard 
Все, которые есть в Python, кроме целочисленного деления.
<!--ID: 1748421292011-->


Какие есть специальные математические операторы в JS? #flashcard 
В JS есть операторы увеличения и уменьшения значения переменной на единицу: `count++` (увеличиваем), `count--` (уменьшаем). Так же есть стандартные сокращения математических операций, например: `count *= 3`, `count += 10`.
<!--ID: 1748421292012-->


Какие особенные операторы сравнения есть в JS? #flashcard 
Тройное равенство и тройное неравенство. Он проверяют идентичность элементов. Пример:
```JavaScript
0 === false;  // => false
false === false;  // => true
```
<!--ID: 1748421292013-->


В чем различие между двойным равенством и тройным равенством в JS? #flashcard 
Разница в том, что двойное равенство проверяет операторы на равенство между собой, а тройное равенство проверяет их идентичность. В частности, при сравнении нуля и `false` через двойное равенство мы получим `true`, т.к., как и в Python, пустые последовательности равны `false`, а при сравнении того же через тройное равенство мы получим `false`, потому что для получения `true` в этом случае нам нужно было бы сравнивать `false` и `false`:
```JavaScript
0 == false;  // => true
0 === false;  // => false
false === false;  // => true
```
<!--ID: 1748421292014-->


Какие есть логические операторы в JS? #flashcard 
Стандартные - И, ИЛИ, НЕ. Но они обозначаются особенными операторами:
```JavaScript
(5 > 1) && (false === false);  // И => true
(5 > 10) || (false === false);  // ИЛИ => true
!(5 > 10);  // НЕ => true
```
<!--ID: 1748421292015-->


Как я могу спросить есть ли нужный мне атрибут в объекте JS? #flashcard 
С помощью оператора `?`. Если есть какой-то атрибут, в существовании которого мы сомневаемся, то можно вызвать его с знаком `?`. В этом случае при отсутствии атрибута JS просто ничего не вернет, а при наличии вернет сам атрибут. Пример:
```JavaScript
x = {a: "Привет!"};
x?.b  // спрашиваем есть ли атрибут b в объекте.
x?.a  // => "Привет!" спрашиваем есть ли атрибут a в объекте.
x.a?.b  // спрашиваем есть ли атрибут b у атрибута a.
```
<!--ID: 1748421292016-->


Как сделать функцию в JS? #flashcard 
С помощью ключевого слова `function`. Функции могут принимать аргументы. Аргументы идут после называния функции в круглых скобках. После аргументов открывается фигурная скобка и пишется тело функции:
```JavaScript
function plus1 (x) {
	return x + 1;
};
```
Вызывается функция стандартным способом - с помощью круглых скобок:
```JavaScript
plus1(10);
```
<!--ID: 1748421292017-->


Можно ли записать функцию в переменную?  #flashcard 
Да, можно - функции могут являться значениями в JS. В этом случае именовать функцию не нужно.
```JavaScript
let square = function (x) {
	return x * x;
};
square(10);
```
<!--ID: 1748421292018-->


Можно ли передавать функции в качестве аргумента другим функциям? #flashcard 
Да, можно. Пример:
```JavaScript
let a, b;
a = function (x) {
	return x * x;
};
b = function (x) {
	return x + x;
};
a(b(10));
```
<!--ID: 1748421292019-->


Как вывести в консоль какую-нибудь информацию из JS скрипта? #flashcard 
С помощью команды `console.log()`.
<!--ID: 1748421292020-->


Какие элементы может хранить объект в JS? #flashcard 
Насколько я знаю на данный момент - любые. И к любому элементу можно получить доступ через точечную нотацию или индексы. Даже списки и другие объекты:
```JavaScript
let x;
x = {
	a: [1, 2, 3],
	b: {c: [1, 2, 3]}
};
x.b.c[0]  // => 1
```
<!--ID: 1748421292021-->


Какие элементы может хранить список в JS? #flashcard 
Насколько я знаю на данный момент - любые. И к любому элементу можно получить доступ через точечную нотацию или индексы. Даже объекты и другие списки:
```JavaScript
let x;
x = [
	1, 2, 3, {a: [1, 2, 3], b: [4, 5, 6]}	
];
x[3].a[0]  // => 1
```
<!--ID: 1748421292022-->

В каких случаях нужно ставить точку с запятой, а в каких она не нужна? #flashcard 
Интерпретатор JS складывает конструкции на разных строках, если может их связать, в одно синтаксически верное предложение на языке. Пример верной программы:
```JavaScript
let a
a
=
3
```
Поэтому точка с запятой ставится в тех случаях, когда мы хотим, чтобы конструкция сверху была четко отделена от конструкции снизу.
<!--ID: 1749898853524-->


Чувствителен ли JS к регистру? #flashcard 
Да.
<!--ID: 1749898853528-->


Что вернет функция, если я применю слово `return`, а на следующей строке после него напишу `true;`? #flashcard 
Слово `return` прерывает выполнение функции, поэтому инструкция `true;` просто не будет прочитана.
<!--ID: 1749898853532-->

Как выйти из консольной утилиты `node`? #flashcard 
```JS
process.exit()
```
<!--ID: 1750430415581-->

Есть ли в JS аналог lambda-функций из Python? #flashcard 
Да, они называются "стрелочные функции":
```JavaScript
const plus1 = x => x + 1;
plus1(2)  // => 3
```
<!--ID: 1750590641363-->


Как создать пустой класс? #flashcard 
С помощью ключевого слова `class`:
```JavaScript
class Test {};
```
<!--ID: 1750590641372-->


Как создать конструктор класса? #flashcard 
С помощью ключевого слова `construct`:
```JavaScript
class Test {
	construct(arg_1, arg_2) {
		this.arg_1 = arg_1
		this.arg_2 = arg_1
	}
}
```
<!--ID: 1750590641375-->


Что означает ключевое слово `this` внутри класса? #flashcard 
Класс - это управляющая конструкция, которая используется, когда нам нужно создать много единообразных объектов. Все объекты обращаются к классу за своими методами и атрибутами. Слово `this` внутри класса - это универсальное обозначение для экземпляра класса. Конструкция `this.arg_1 = arg_1` обозначает, что для экземпляра класса, который "спрятан" внутри слова `this` нужно определить свойство `arg_1`.
<!--ID: 1750590641378-->


На какие группы можно разделить встроенные типы JS? #flashcard 
Таких группы всего две:
1) элементарные типы - строки, числа, булевские значения, `null`, `undefined`.
2) объектные типы - объекты, массивы + некоторые узкоспециализированные объектные типы (`RegExp`, `Date`, `Set`, `Map`).
<!--ID: 1750590641381-->


Как добавить элемент в уже существующий массив JS? #flashcard 
С помощью метода `push()`:
```JavaScript
> let a = [1, 2, 3]
undefined
> a.push(1)
4
> a
[ 1, 2, 3, 1 ]
```
<!--ID: 1750590641385-->


Как перевернуть массив? #flashcard 
С помощью метода `reverse()`:
```JavaScript
> let b = [1, 2, 3]
undefined
> b.reverse()
[ 3, 2, 1 ]
```
<!--ID: 1750590641388-->

Что такое "объект" в JS? #flashcard 
Объект - это коллекция свойств, где каждое свойство имеет имя и значение (либо элементарное значение, либо другой объект). Пример:
```JavaScript
let a = {name: 'Peter', second_name: 'Petukhov', birth_date: '25.01.2000'};
```
<!--ID: 1750685768615-->


Что такое свойство объекта в JS? #flashcard 
Это пара имя-значение.
<!--ID: 1750685768618-->


Являются ли функция и класс значениями, которыми можно манипулировать? Если да, то в каком смысле можно это делать? #flashcard 
В Python все функции и классы так же являются объектами - это означает, что их можно использовать, как элементы списка, например. В JS есть похожая история - функциями и классами можно манипулировать, как всеми прочими значениями:
```JavaScript
const x = x => x + 1;
let a = [];
a.push(x);
a  // => [[Function: x]]
```
<!--ID: 1750685768620-->


Как долго хранятся объекты JS в памяти? #flashcard 
В JS существует свой сборщик мусора, который чистит память от тех объектов, на которые перестали ссылаться из работающей программы.
<!--ID: 1750685768623-->


Как обозначаются константы и переменные в JS? #flashcard 
`let` - для переменных, а `const` - для констант. В JS нет строгой привязки к типу, поэтому при объявлении переменных его не указывают. После создания переменной или константы в них можно сохранить любое значение.
<!--ID: 1750685768626-->
