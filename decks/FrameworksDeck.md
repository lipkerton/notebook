TARGET DECK: FrameworksDeck

Что нужно импортировать в файле Python, чтобы получить возможность написать минимальный сервер flask? #flashcard 
Нужно импортировать из flask класс `Flask`:
```Python
from flask import Flask
```
<!--ID: 1748260071574-->


Какой объект нужно создать, чтобы запустить минимальный сервер + задействовать в нем статику (Flask)? #flashcard 
Нужно сделать экземпляр класса `Flask` и передать в него параметры `__name__`, `static_folder`, `static_url_path`:
```Python
app = Flask(
	__name__, static_folder='./static', static_url_path='/static'
)
```
<!--ID: 1748260071579-->


Для чего нужен аргумент `static_folder` классу `Flask`? #flashcard 
Он нужен, чтобы прописать относительный (или абсолютный) путь до папки, которая будет хранить файлы статики. Это необходимо, чтобы метод `send_static_file()` и другие подобные знали откуда брать статику, чтобы отправить ее пользователю.
<!--ID: 1748260071583-->


Для чего нужен аргумент `static_url_path` классу `Flask`? #flashcard 
В сущности это указатель на папку со статикой в URL-формате. Это означает, что это путь от корневой папки проекта (от папки в которой лежит исполняемый файл). Через этот аргумент HTML-файлы в первую очередь будут искать статику. Однако он не обязательный - если этого аргумента не будет, то статика будет искаться по аргументу `static_folder`.
<!--ID: 1748260071587-->


Для чего нужен метод `send_static_file()` и что в него нужно передавать? К экземпляру какого класса он применяется? #flashcard 
Это метод, который возвращает пользователю статический файл. Передать в этот метод нужно имя статического файла, чтобы он нашел его в папке статики. Этот метод применяется к экземпляру класса `Flask`.
<!--ID: 1748260071589-->


Для чего нужен метод `run()` и что в него нужно передавать? К экземпляру какого класса он применяется? #flashcard 
Это метод, который запускает сервер и заставляет его слушать запросы пользователей. Передать в него можно аргументы `port` и `debug`. Аргумент `port` определяет на каком порте сервер будет слушать запросы. Аргумент `debug` принимает либо `True`, либо `False`; если значение `True`, то сервер запустится в режиме разработки - это означает, что любая ошибка обращения к ресурсу будет подробно описана.
<!--ID: 1748260071593-->


Что такое `@app.route()`? #flashcard
Это декоратор и маршрутизатор. В скобки передается путь, например, `/` или `/catalog`, - это путь, на который пользователь может послать запрос. Маршрутизатор перекинет запрос пользователя в функцию над, которой стоит соответствующий маршрутизатор. `app` - это экземпляр класса `Flask`. Если пользователь обращается к ресурсу `catalog`, то его запрос будет перекинут в функцию `catalog` таким образом:
```Python
@app.route('/catalog')
def catalog():
	pass
```
<!--ID: 1748260071596-->


Чем таким фреймворк Flask отличается от фреймворка Bottle? И как они вообще связаны? #flashcard 
Разработчик Flask назвал фреймворк именно так, как он называется, потому что обыгрывал название другого фреймворка - Bottle. Отличаются они тем, что у Flask есть более широкая система управления шаблонами - jinja2.
<!--ID: 1748260071599-->


Как называется по умолчанию папка для статики в Flask? #flashcard 
`static_folder`
<!--ID: 1748260071603-->


Как называется по умолчанию папка для хранения шаблонов в Flask? #flashcard 
`templates`
<!--ID: 1748260071605-->


Что вообще такое шаблон и зачем он нужен? #flashcard 
Это документ с наиболее распространенной структурой сайта в формате HTML, в который можно вставить требуемые штуки. Это нужно, чтобы не тратить время на заполнение самых основных элементов, которые есть на каждой веб-странице.
<!--ID: 1748260071608-->


С помощью какой функции мы можем зарендерить шаблон (Django, Flask)? #flashcard 
`render_template()` (Flask) - она принимает на вход только название шаблона, который она будет отправлять клиенту в ответ на запрос. Дополнительно в эту функцию можно передать аргументы.
`template.render(context, request)` (Django) - `template` в данном случае является объектом класса `Template`, который создается с помощью метода `get_template` объекта `loader`.
`render(request, template)` (Django) - `render()`является функцией из модуля `django.shortcuts`;  по сути она является сокращением предыдущей функции. 
<!--ID: 1748260071612-->


Как можно передать аргументы от клиента в функцию на сервер? #flashcard 
Два варианта:
1) Прямолинейно записать в URL в таком формате:
```
http://www.example.com/echo/<name>/<title>
```
2) Передать вместе с параметрами GET-запроса:
```
http://www.example.com/echo?name=Peter&title=Cool
```
<!--ID: 1748260071616-->


Как можно вытащить аргументы из запроса внутри функции Flask? #flashcard 
Если это аргументы, которые находятся в самом запросе, то таким образом:
```Python
@app.route('/echo/<name>/<title>')
def echo(name, title):
	pass
```
А если это аргументы, которые переданы в качестве query-параметров, то таким:
```Python
@app.route('/echo/')
def echo():
	from flask import request
	name = request.args.get('name')
	title = request.args.get('title')
```
<!--ID: 1748260071619-->

Как мы запишем маршрут для функции, которая должна принимать на вход индекс товара в каталоге (Flask)? #flashcard 
Обращаемся к экземпляру класса Flask и вызываем функцию `route()`:
```Python
@app.route('/catalog/<index>')
def catalog_item(index):
	return 'Это предмет из каталога!'
```
<!--ID: 1751034622400-->


Как использовать конвертер пути в маршрутах Flask? #flashcard 
Его нужно использовать напрямую в описании маршрута:
```Python
@app.route('/<int:item>')
```
<!--ID: 1751034622401-->


Как начать проект на Django? #flashcard 
Нужно прописать в терминале:
```Bash
django startproject <имя проекта>
```
<!--ID: 1748421291994-->


Как запустить сервер разработки на Django? #flashcard 
Нужно обратиться к файлу `manage.py` и написать `runserver`:
```Bash
python manage.py runserver
```
<!--ID: 1748421291995-->


Как создать Django-приложение? #flashcard 
Нужно обратиться к файлу `manage.py` и написать команду `startapp`:
```Bash
python manage.py startapp <имя приложения>
```
Папка с приложением появится в той директории, из которой была вызвана команда. После создания нужно зарегистрировать новое приложение через константу `INSTALLED_APPS` в `settings.py`.
<!--ID: 1748421291996-->


Как можно настроить отдельный маршрутизатор (отдельный `urls.py`) для каждого приложения (Django)? #flashcard 
Нужно в главном `urls.py`, который находится рядом с файлом `settings.py`, пробросить маршруты в отдельные для каждого приложения `urls.py` с помощью функции `include`:
```Python
from django.urls import path, include
urlpatterns = [
	path('', include('homepage.urls')),
	path('catalog/', include('catalog.urls'))
]
```
При этом в самих приложениях `urls.py` будет выглядеть так (на примере `catalog`):
```Python
from django.urls import path
from . import views
urlpatterns = [
	path('', views.catalog_main_page)
]
```
<!--ID: 1748421291997-->


Как передать переменную из URL в функцию (Django)? #flashcard 
Чтобы это сделать нужно прописать в маршрутизаторе шаблон такого вида: `catalog/<pk>/`. Django будет считать, что он может подставить на место `<pk>` какое-нибудь значение из полученного запроса. Далее, для шаблона вызывается функция и эта функция по умолчанию обязательно будет принимать аргумент (даже если он не будет использован внутри нее):
```Python
def func_with_args(request, pk):
	pass
```
<!--ID: 1748421291998-->


Как сделать шаблон, который в качестве аргумента будет принимать только числа (Django)? #flashcard 
Использовать конвертер пути `int`: `catatlog/<int:pk>/`. При такой записи Django поймет, что шаблону соответствует только URL с числом внутри.
<!--ID: 1748421291999-->


Что такое конвертер пути (Django)? #flashcard 
Это способ проверить можно ли преобразовать часть пути, который получен вместе с запросом, в какой-нибудь тип данных. Например, если мы ожидаем в одном из шаблонов получить число, то в этом шаблоне мы можем проверить пришедший запрос на соответствие именно с числом: `catalog/<int:pk>/`.
<!--ID: 1748421292000-->


Как базово устроена маршрутизация запросов в Django? #flashcard 
От пользователя приходит запрос `HttpRequest`. Этот запрос просит конкретный ресурс на сайте по ссылке. Сначала он отправляется в маршрутизатор (`urls.py`) - ссылка поочередно сравнивается с шаблонами, которые есть в `urls.py`. Как только находится подходящий под ссылку шаблон объект `HttpRequest` отправляется в view-функцию для этого шаблона. View-функция обрабатывает запрос и возвращает в ответ объект`HttpResponse`.
<!--ID: 1748421292001-->


Что такое миграции в Django? #flashcard 
Миграции в Django - это инструмент, который позволяет разработчику не писать сырые SQL запросы, а прописать все что нужно в виде моделей на Python. После того как модели описаны Django может автоматически создать всю необходимую структуру в выбранной СУБД. Формально таким образом Django сам управляет БД, создавая в ней таблицы и связи.
<!--ID: 1748421292002-->


Что нужно сделать, чтобы применить миграции (Django)? #flashcard 
Прежде чем применить миграции нужно сначала создать файлы миграций, т.е. сначала мы проанализируем существующие модели и напишем файлы для будущих миграций. Это можно сделать с помощью команды:
```Bash
python manage.py makemigrations
```
Далее, можно применить сами миграции на основе файлов миграций:
```Bash
python manage.py migrate
```
<!--ID: 1748421292003-->


Как зарегистрировать приложение в Django? #flashcard 
Нужно в константе `INSTALLED_APPS`, которая находится в `settings.py`, прописать путь до класса, который прописан в файле `apps.py` в каталоге приложения. Это может выглядеть, например, так: `catalog.apps.CatalogConfig`. `CatalogConfig` в данном случае является именем класса.
<!--ID: 1748421292004-->


Как выглядят параметры GET запроса? #flashcard 
Под "параметрами запроса" имеются в виду аргументы, которые записываются после тела запроса. От дела запроса они отделяются знаком `?` и далее маркируются в формате `<имя_аргумента>=<значение_аргумента>`. Друг от друга они отделяются знаком `&`:
```
http://localhost:6789/catalog?item=1488&size=228
```
<!--ID: 1748876290881-->


Как изменить стандартную папку для хранения шаблонов в Flask? #flashcard 
Нужно при создании экземпляра класса `Flask` указать атрибут `template_folder` и прописать в нем путь до новой папки. Путь может быть либо абсолютным (от корня системы), либо относительным - относительно текущего файла.
<!--ID: 1748876290882-->


Как указать папку для хранения html-шаблонов в Django? #flashcard 
Нужно обновить константу `TEMPLATES`. Она хранит словарь с настройками шаблонизатора, в частности, она хранит ключ `DIRS` со списком, который содержит пути до директорий с шаблонами. Внутри нужно прописать системный путь до директории с шаблонами.
<!--ID: 1749048050805-->


Что такое `context_processors` в Django? #flashcard 
Во-первых эта сущность появляется в настройках `TEMPLATES` в файле `settings.py`. Во-вторых, она описывает несколько разных функций, которые работают с объектом `HttpRequest`, чтобы при рендеринге достать из него какие-то дополнительные сведения, которые нужны для словаря контекста. Именно поэтому функциям рендеринга всегда отдается на вход, помимо всего прочего, еще и сам пользовательский запрос.
<!--ID: 1749640112643-->


Что такое рендеринг в веб-фреймворках? #flashcard 
Это когда мы делаем из шаблона, который модифицирован с помощью языка шаблонизатора, готовый HTML файл. При рендеринге мы, например, вставляем аргументы в шаблон из словаря контекста + сшиваем готовый HTML из кусков и проч.
<!--ID: 1749640112666-->


Какой шаблонизатор по умолчанию используется в Django? #flashcard 
Django Templates, но можно еще использовать jinja2.
<!--ID: 1749640112671-->


Как избежать написания отдельных полноценных HTML документов для всех страниц сайта? #flashcard 
Шаблонизатор позволяет с помощью специального синтаксиса настроить взаимосвязанную систему шаблонов. Большая часть контента неизменна в HTML документах в рамках одного сайта, поэтому ее можно вынести в отдельный файл. При этом весь контент, который меняется будет лежать в индивидуальных для каждого ресурса файлах. Например, в Django Templates я могу сделать `base.html`, где есть все, кроме контента тега `<main></main>`; сам контент тега лежит по файлам типа `catalog.html` или `catalog_item.html`.
<!--ID: 1749640112675-->


Как в Django Tempalates убрать повторяющиеся блоки кода в шаблонах? Я не хочу в шаблоне для каждой страницы на своем ресурсе писать кодировку. #flashcard 
Это можно реализовать с помощью синтаксиса `{% extends "имя шаблона" %}` и `{% block имя блока %}{% endblock %}`
<!--ID: 1749640112719-->

Какие есть управляющие конструкции в Django Templates? #flashcard 
1) for
2) if
<!--ID: 1749898853537-->


Как передать на рендеринг список и перебрать его содержимое на странице? #flashcard 
Передаем список в словаре контекста на рендеринг под каким-нибудь ключом. Внутри шаблона прописываем управляющую конструкцию `for` и внутри нее обращаемся к списку по ключу в словаре контекста:
```HTML
{% for item in items %}
  <p>{{ item }}</p>
{% endfor %}
```
<!--ID: 1749898853541-->


Какой формат создания управляющей конструкции `if`в Django Templates? #flashcard 
Фигурная скобка + знаки процента + пробелы + конструкция внутри. В конце управляющей конструкции обязательно ставится `endif` в идентичном формате. Так же может быть использован `else` и `elif`:
```HTML
{% if content == 'Привет' %}
  <!-- пишем теги -->
{% elif content == 'Пока' %}
  <!-- пишем теги -->
{% else %}
  <!-- пишем теги -->
{% endif %}
```
<!--ID: 1749898853545-->


Какой формат создания управляющей конструкции `for`в Django Templates? #flashcard 
Фигурная скобка + знаки процента + пробелы + конструкция внутри. В конце управляющей конструкции обязательно ставится `endfor` в идентичном формате.
```HTML
{% for item in items %}
  <!-- пишем теги -->
{% endfor %}
```
<!--ID: 1749898853549-->

Что такое `forloop`? #flashcard 
Это переменная в циклах Django Templates. По ней можно получить доступ к дополнительным фишкам цикла `for`. Например, я могу спросить "Последний ли это шаг?" внутри цикла с помощью конструкции `forloop.last`:
```HTML
{% for item in items %}
  {% if forloop.last %}
    <!-- какие-то действия -->
  {% endif %}
{% endfor %}
```
<!--ID: 1750061689584-->


Как сделать счетчик внутри цикла в Django Templates? #flashcard 
Двумя способами:
1) `forloop.counter` - счетчик с единицы.
2) `forloop.counter0` - счетчик с нуля.
<!--ID: 1750061689588-->


Что может перебрать цикл `for` в Django Templates? #flashcard 
Он может перебрать любые итерируемые последовательности в Python.
<!--ID: 1750061689590-->

Что делать если есть случай, когда в цикл шаблонизатора Django Templates попадает пустая последовательность? #flashcard 
Поведение цикла можно адаптировать под эту ситуацию с помощью тега `empty` - это дополнительный тег цикла `for` в шаблонизаторе:
```HTML
{% for item in items %}
  <!-- здесь код -->
{% empty %}
  <!-- здесь код на случай пустой последовательности -->
{% endfor %}
```
<!--ID: 1750344982545-->


Я передал в словарь контекста список, который состоит из словарей. В словарях хранится два ключа - `"name"` и `"index"`. Как мне перебрать этот список в Django Templates и сделать для каждого словаря в списке ссылку, где в заголовке будет фраза "Это товар под именем `name`" (сама ссылка должна отправлять на индивидуальную карточку товара через использование индекса)? #flashcard
Чтобы это сделать я воспользуюсь дополнительным параметром функции `path` - `name`. В `urls.py` я отмечу маршрут до страницы с карточкой товара так:
```Python
path('catalog/<int:index>/', views.item_page, name='product_detail')
```
А в шаблоне пропишу следующее:
```HTML
<ul>
{% for item in items %}
  <li>
    <a href="{% url 'product_detail' item.index %}">
      Это товар {{ item.name }}
	</a>
  </li>
{% empty %}
  <h1>Товаров нет.</h1>
{% endfor %}
</ul>
```
<!--ID: 1750344982549-->


У меня есть куча товаров на странице каталога и каждый товар должен ссылаться на свою индивидуальную страницу. Я не хочу описывать для каждого товара полную ссылку в теге `<a>` (например, `<a href='catalog/{{ index }}/>)'`. Можно ли как-то изменить этот механизм в Django Templates? #flashcard 
Да, можно. Чтобы изменить это поведение мы можем прописать атрибут `name` для функции `path` в `urls.py`:
```Python
path('catalog/<int:index>/', views.items, name='product_detail')
```
Имя, которое записано в `name` можно использовать в теге `a`:
```HTML
<a href="{% url 'product_detail' <аргументы> %}"></a>
```
При рендеринге на место конструкции шаблонизатора будет подставлен путь на ресурс из `urls.py`, на который ссылается имя `product_detail`.
<!--ID: 1750344982557-->


Как сослаться на элемент списка, который был передан в словаре контекста под ключом `items`, внутри шаблона Django Templates? #flashcard 
С помощью точечной нотации и номера индекса: `{{ items.0 }}`.
<!--ID: 1750344982563-->


Как сослаться на элемент словаря, который был передан в словаре контекста под ключом `items`, внутри шаблона Django Templates? #flashcard 
С помощью точечной нотации и имени ключа во вложенном словаре: `{{ items.key }}`.
<!--ID: 1750344982567-->


Могу ли я передать в шаблон Django Templates экземпляр класса и взять значение одного из атрибутов в нем? #flashcard 
Да, делается это точно так же, как и с значениями словаря - обращение через точечную нотацию и имя атрибута: `{{ item.attribute }}`.
<!--ID: 1750344982572-->

В Django Templates я пользуюсь такой конструкцией `<a href={% url 'product_detail' <переменная> %}`. Туда, где написано `<переменная>` я хочу вставить какой-то аргумент в "стиле" Django Templates (например, `{{ title }}`). Нужно ли мне для этого вставлять аргумент используя двойные фигурные скобки? #flashcard 
Нет, аргумент вставляется безо всего.
<!--ID: 1750345950452-->

Как выполнить сырой запрос в Django в функции в `views.py`, если у меня нет моделей? #flashcard 
В данном случае интерфейс будет похож на итерфейс модуля `sqlite3` в Python:
```Python
from django.db import connection
def test_1(request):
	with connection.cursor() as cursor:
		cursor.execute('<запрос на SQL>')
		result = cursor.fetchall()
		print(result)
```
<!--ID: 1750430415584-->

Как зарендерить шаблон в Django? #flashcard 
Два способа:
```Python
from django.template import loader
from django.http import HttpResponse
from django.shortcuts import render
def test(request):
	# вариант loader
	template = loader.get_template('test/test.html')
	context = {}
	return HttpResponse(template.render(context, request))
	# вариант render
	template = 'test/test.html'
	context = {}
	return render(request, template, context)
```
<!--ID: 1750527557205-->

Что делать, если я хочу добавить одинаковые `name` в `path()` для разных приложений проекта Django? #flashcard 
Можно воспользоваться `namespace` - это функционал, который позволяет внутри шаблона обращаться не только к значению `name`, а к значению `name` через имя приложения, в котором оно прописано:
```HTML
<a href="{% url 'catalog:product_list' %}">Страница каталога</a>
```
Чтобы это было возможным нужно прописать константу с именем приложения в `urls.py` приложения:
```Python
app_name = 'catalog'
...
path('catalog/', views.catalog_main, name='product_list')
```
<!--ID: 1750685768629-->

Как запустить сервер и создать простейшую страницу с помощью FastAPI? #flashcard 
Для начала нужно установить FastAPI + его ASGI сервер Uvicorn:
```Bash
python -m pip install "fastapi[standard]"
```
Теперь создаем файл `main.py`:
```Python
from fastapi import FastAPI
app = FastAPI()
@app.get("/")
def homepage():
	return {"message": "Hello world!"}
```
И запускаем его на сервере:
```Bash
fastapi dev main.py
```
<!--ID: 1751034622402-->

Как добавить конвертер пути в маршрут FastAPI? #flashcard 
В FastAPI есть сразу два типа конвертера маршрута: 1) в самом маршруте, 2) указание типа с помощью стандартного интерфейса Python.
```Python
@app.get("/catalog/{item_id:int}")
def catalog_item(item_id:int):
	pass
```
<!--ID: 1751360245408-->


У меня в FastAPI есть маршрут `@app.get("/catalog/{item_id}")` и я хочу, чтобы для него подходили только запросы, которые вместо `{item_id}` содержат число. Как я могу реализовать такой подход? #flashcard 
Я могу сообщить конвертер пути для маршрута и указать его же в параметрах функции. Важно помнить, что если конвертер будет указан только в параметрах функции с помощью типизации, то могут возникать коллизии, например, между такими функциями:
```Python
@app.get("/catalog/{category_slug}")
def catalog_category(category_slug:str):
	pass
@app.get("/catalog/{item_id}")
def catalog_item(item_id:int):
	pass
```
Коллизия возникнет при запросе на эндпоинт `/catalog/15` - этот путь попадет не во вторую, а в первую функцию, но не пройдет проверку типа (функция ожидает `str`) и вернет ошибку.
<!--ID: 1751360245413-->


Я хочу указать для функции FastAPI ограниченный набор вариантов того, что может быть в нее передано с помощью параметров пути. Как я могу это сделать? #flashcard 
С помощью модуля `enum` стандартного пакета Python - нужно определить собственный класс и унаследовать его от `Enum`, а затем в параметрах функции указать для параметров этот класс в качестве типа:
```Python
from enum import Enum
class MyClass(Enum):
	pass
@app.get("/catalog/{item_slug}")
def catalog_item(item_slug:MyClass):
	pass
```
<!--ID: 1751360245416-->


Как передаются query-параметры внутрь функции FastAPI? #flashcard 
Query-параметры принимаются функцией FastAPI без какого-то дополнительного синтаксиса:
```Python
@app.get("/catalog/")
def catalog(category:str=None, size:int=None):
	pass
```
`category` и `size` примут значения, когда они будут переданы с помощью запроса по этому эндпоинту:
```
http://127.0.0.1:8000/catalog?category=table&size=big
```
<!--ID: 1751360245419-->


Какие параметры есть в Django Templates у переменных и как указать для переменной параметр? #flashcard 
Внутри шаблона параметр указывается так:  `{{ <переменная>|<параметр> }}`
Их много, но основные:
1) `length` - вычисляет длину строки.
2) `safe` - убирает экранирование с текста (никогда нельзя использовать).
3) `linebreaksbr` - переводит разрывы строк с языка Python на HTML/
4) `date:` - после двоеточия позволяет указать формат времени. Например: `{{ <переменная>|date:"d.m.y" }}`.
5) `title` - делает все первые буквы слов заглавными.
6) `truncatewords:` - после двоеточия указывается цифра, которая обозначает лимит слов в строке. То есть строка будет урезана до такого количества слов.
<!--ID: 1751360245422-->

Как называется приложение, которое отвечает за раздачу статических файлов в Django? #flashcard 
`django.contrib.staticfiles` его можно увидеть в списке `INSTALLED APPS`.
<!--ID: 1751560913713-->


Как прописать место для хранения статики в Django? #flashcard 
Нужно задать константу `STATICFILES_DIRS` в `settings.py`, где будет храниться список с перечислением всех папок со статикой по проекту.
```Python
STATIC_DIR = BASE_DIR / 'static'
STATICFILES_DIRS = [
	STATIC_DIR
]
```
<!--ID: 1751560913716-->


Как в шаблоне Django Templates использовать статику? #flashcard 
Чтобы использовать статику в шаблоне нужно прописать `{% load static %}` + нужно указать в теге `link` ссылку на статику:
```HTML
<link rel="stylesheet" href="{% static 'css/my_static.css' %}">
```
Указать `link` можно в `base.html` - он унаследуется во все дочерние шаблоны, а вот конструкция `{% load static %}` не наследуется, поэтому ее нужно указывать для каждого шаблона индивидуально.
<!--ID: 1751560913719-->


Как добавить картинку из статики на страницу в Django Templates? #flashcard 
Надо добавить в документ тег `{% load static %}` + дать ссылку на картинку в папке статики:
```HTML
<img src="{% static 'img/logo.png' %}">
```
<!--ID: 1751560913722-->


Как в FastAPI разделить единый файл с проектом на структуру приложения? #flashcard 
Для начала нужно сделать необходимую структуру директорий в папке проекта. Нельзя забыть про файл `__init__.py` (в каждой папке с проектом), который позволяет делать импорты в пределах директории проекта. В главном файле `main.py` у нас должен быть создан экземпляр класса `FastAPI`, но в дочерних мы создаем экземпляр класса `APIRouters` - в дочерних файлах мы будем ссылаться как раз на него при создании всех декораторов. Затем в главном файле нам просто нужно обратиться к экземпляру класса `FastAPI` и вызвать метод `include_router()`. В качестве аргумента ему передается ссылка на экземпляр класса `APIRouters` из дочернего файла. У меня это выглядит так:
```Python
from app.routers import test_1
app = FastAPI()
app.include_routers(test_1.router)
```
<!--ID: 1751560913725-->


Я хочу передать в качестве query-параметра булевское значение. Какие значения будут приведены к `True` в FastAPI автоматически? #flashcard 
`True, true, yes, 1, on`
<!--ID: 1751560913728-->


В каких случаях query-параметр в FastAPI считается обязательным? #flashcard 
В тех случаях, когда для него не указано значение по умолчанию в параметрах функции.
<!--ID: 1751560913731-->


Можно ли в FastAPI передать путь в качестве параметра пути? #flashcard 
Можно, но для этого нужно использовать конвертер пути `path`:
```Python
@app.get("/file/{file_path:path}")
```
<!--ID: 1751560913734-->

Как сделать в FastAPI функцию, которая принимает POST запросы? #flashcard 
Для начала нужно поменять декоратор для функции и указать в нем путь, который будет обрабатывать POST запросы:
```Python
@app.post("/items")
```
Затем у нас должен быть создан класс Pydantic, который будет проверять входящие данные (тело запроса) и конвертировать их их JSON формата в формат обычного экземпляра класса Python:
```Python
from pydantic import BaseModel
class Item(BaseModel):
	name: str
	price: float
```
Теперь можно использовать имя этого класса, чтобы указать аннотацию для POST функции:
```Python
@app.post("/items")
def new_item(item: Item):
	return item
```
<!--ID: 1751880081015-->


Как написать POST запрос к эндпоинту? #flashcard 
Я делаю это через утилиту `curl`:
```Bash
curl -X POST -H "Content-Type: application/json" -d '{"name": "Potato", "price": 45.1}' http://127.0.0.1:8000/items
```
<!--ID: 1751880081016-->


Как можно в FastAPI написать дополнительную проверку для query-параметра (например, проверку на длину передаваемой в него строки)? #flashcard 
С помощью стандартной аннотации типов в Python:
```Python
from typing import Annotated
from fastapi import Query
@app.get("/user_info")
def user_info(
	name: Annotated[str | None, Query(min_length=1, max_length=50)] = "Joe",
	email: Annotated[str | None, Query(min_length=1, max_length=50)] = "Doe"
):
	return {"name": name, "email"}
```
<!--ID: 1751880081017-->


Как проверить, что значение числового query-параметра в FastAPI не меньше 1 и не больше 1000? #flashcard 
Для этого можно использовать аннотацию типов и `Query`:
```Python
from typing import Annotated
from fastapi import Query
def my_func(
	number: Annotated[int, Query(gt=0, le=1000)]
):
	pass
```
Параметры:
1) `gt` - greater then
2) `le` - lesser or equal
<!--ID: 1751880081018-->

Что такое сессия в SQLAlchemy и как ее создать? #flashcard 
Сессия - это штука, которая позволяет отправлять запросы в БД не по одному, а сразу пачкой в момент выполнения `commit()` (такая механика называется `Unit of Work`) + сессия следит за теми данными, которые попадают из БД в код, например, она не дает делать дубликаты данных в коде, чтобы не забивать память (`Identity Map`).
Чтобы создать сессию:
```Python
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
engine = create_engine()
session = sessionmaker(bind=engine)
```
<!--ID: 1752497390631-->


Как можно верифицировать входящие данные в функцию FastAPI? #flashcard 
Можно сделать Pydantic схему, например:
```Python
from pydantic import BaseModel
class UserSchema(BaseModel):
	username: str
```
Теперь эту схему можно передать в качестве определения типа в функцию FastAPI:
```Python
@app.post("/user")
def add_user(user: UserSchema, session: SessionDep):
	new_user = models.User(
		username=user.username
	)
	session.add(new_user)
	await session.commit()
```
<!--ID: 1752497390636-->

Что такое авторизация? #flashcard
Это процесс, который определяет какие действия разрешено делать данному пользователю на ресурсе. Например, анонимному пользователю можно смотреть посты, но нельзя их писать.
<!--ID: 1752497390639-->

Что такое аутентификация? #flashcard 
Это процесс проверки, который выясняет является ли пользователь тем, за кого себя выдает. Обычно для прохождения проверки нужно ввести какие-то данные или подтвердить личность через сторонние ресурсы.
<!--ID: 1752497390643-->

Что такое JWT? #flashcard 
Это метод организации аутентификации по токену. JWT токен - это JSON, который состоит из трех компонентов: `header` (содержит алгоритм шифрования токена и тип токена), `payload` (содержит что угодно, например, юзернейм клиента, пароли, статус на сервере и проч.), `signature` (закодированные поля `header` + `payload`). Чтобы стать реальным токеном этот JSON сверху кодируется с помощью какого-нибудь протокола шифрования. Фишка JWT в возможности его "распаковать" и достать оттуда сведения о пользователе, которые нам нужны, например, его статус на сервере, т.е. не надо лезть в БД.
<!--ID: 1756042523978-->

Как авторизоваться с помощью `Basic` через `curl`? #flashcard 
```Bash
curl -X POST http://127.0.0.1:8000/login --user peter:1488
```
<!--ID: 1756042523981-->

Как авторизоваться с помощью `Bearer` через `curl`? #flashcard 
Чтобы авторизоваться с помощью JWT-токена нужно передать его в заголовок `Authorization`:
```Bash
curl -X GET -H "Authorization: Bearer <токен>" http://127.0.0.1:8000 
```
<!--ID: 1756042523983-->

Какой класс из `fastapi` используется, чтобы проверять входящие запросы на наличие Bearer-токена? #flashcard 
```Python
from typing import Annotated
from fastapi import HTTPBearer
security = HTTPBearer()
credentials = Annotated[str, Depends(security)]
```
<!--ID: 1756042523985-->

Как создать миграцию alembic? #flashcard 
```Bash
alembic revision --autogenerate -m "init"
```
<!--ID: 1760856190586-->


Как очистить историю миграций alembic? #flashcard 
В папке `migrations/versions` нужно удалить все файлы миграций + нужно удалить из БД историю миграций, которая лежит в `alembic_versions` - лучше просто стереть всю таблицу. Удалить миграции точечно не получится, потому что алембик наследует одну миграцию от другой и убрать из этой цепочки компонент нельзя.
<!--ID: 1760856190587-->
