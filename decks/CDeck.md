TARGET DECK: CDeck

Что такое указатель? #flashcard 
Указатель это штука, которая указывает на конкретную область в памяти. Например:
```C
int a = 1;
int *p;
p = &a;
```
Здесь я объявляю указатель `int *p`, который должен указывать на адрес памяти, где хранится значение типа `int`. Потом я получаю адрес памяти значения от переменной `a` - `&a` и записываю его в указатель.
<!--ID: 1770141538781-->


Какого размера может быть указатель? #flashcard 
1-8 байт.
<!--ID: 1770141538784-->


Как я могу сделать указатель на указатель? #flashcard 
```C
int a = 1;
int *p = &a;
int **d = &p;  // указатель на указатель.
```
<!--ID: 1770141538787-->


У меня есть переменная `int a = 1;`. Что означает запись `&a`? #flashcard 
Так я получаю адрес единицы в памяти, т.е. адрес значения переменной в памяти.
<!--ID: 1770141538790-->


Если я напишу такой код `*p = &a` при том, что у меня есть переменная `int a = 1;` и указатель `int *p = &a;`, что произойдет? #flashcard
В этом случае откроется ячейка памяти, на которую показывает указатель `p` и туда будет записано числовое значение адреса значения переменной `a`.
<!--ID: 1770141538793-->

Что будет если я вычту из указателя единицу? #flashcard 
Зависит от типа указателя. Если мой указатель имеет тип `int`, то при вычитании я сделаю шаг в четыре байта по памяти.
<!--ID: 1771051251717-->


Как я могу перемещаться по массиву в C? #flashcard 
С помощью арифметики указателей:
```C
int values[5] = {10, 20, 30, 40, 50};  // массив из пяти элементов.
printf("%p\n", values);  // указатель на первый элемент массива.
printf("%p\n", values + 1);  // указатель на второй элемент массива.
printf("%d\n", *(values + 1));  // второе число в массиве.
```
Или с помощью индексов (что то же самое):
```C
int values[5] = {10, 20, 30, 40, 50};
printf("%d\n", values[1]);  // второе число в массиве.
```
<!--ID: 1771051251723-->

Как устроен цикл `for` в С? #flashcard
```C
for (инициализация; условие; итерация) {
	...
}
```
В блоке инициализации я объявляю одну или несколько локальных переменных цикла. В блоке условия я описываю условие завершения цикла. В блоке итерации я говорю о том, что будет происходить каждый поворот цикла.
<!--ID: 1771051794712-->

Как занять место в heap памяти под массив из десяти чисел типа `float`? #flashcard 
С помощью функции `malloc()`:
```C
float* dest = malloc(10 * sizeof(float));
```
<!--ID: 1771058481077-->


Что возвращает функция `malloc()`? #flashcard 
Она возвращает указатель на первый байт выделенного места в heap.
<!--ID: 1771058481083-->


Как я могу освободить место в heap, которое я занял ранее с помощью `malloc()`? #flashcard 
С помощью функции `free()`. Я должен передать в нее указатель, который ранее вернула функция `malloc()`.
<!--ID: 1771058481086-->


Как выполнить приведение типа? И в чем особенность такого приведения? #flashcard
Особенность в том, что приведение - это не преобразование. Приведение типа говорит компилятору как смотреть на данные в этой ячейке, не меняя их. То есть такой синтаксис:
```C
int a = 1;
printf("%f", (float) a);
```
<!--ID: 1771156451635-->

Как выполнить преобразование типа (на примере чисел)? #flashcard 
Чтобы выполнить преобразование числа типа `float` к числу типа `int`:
```C
float a = 1.0f;
int b = (int) a;  // явное преобразование типа.
int c = a;        // неявное преобрзование типа.
```
<!--ID: 1771419569681-->


В чем сложность преобразования строки к числу? #flashcard 
В C нет типа `string` - есть только тип `char`, который может репрезентировать один символ. Строка является последовательностью байт в памяти, а последовательности в C репрезентированы указателем на память, где хранится их первый элемент. Поэтому строку можно преобразовать специальной функцией:
```C
int a = atoi(string);
```
<!--ID: 1771419569684-->


Что делает такая запись `a |= 1`? #flashcard 
Оператор `|=` это побитовое ИЛИ с присваиванием.
Например:
```C
int a = 0;
a |= 1;  // 00000000 | 00000001 = 00000001 = 1;
a |= 2;  // 00000001 | 00000010 = 00000011 = 3;
a |= 1;  // 00000011 | 00000001 = 00000011 = 3;
```
Побитовое ИЛИ сравнивает каждый бит двух чисел по правилу - если один из двух битов равен `1`, то результат равен `1`. Если оба бита равны `0`, то результат равен `0`.
<!--ID: 1771156451638-->


Что такое `volatile`? #flashcard 
Это ключевое слово ставиться перед объявлением переменной для того, чтобы сообщить компилятору, что значение переменной может быть изменено в любой момент времени извне кода программы. Например:
```C
volatile int a = 5;
```
<!--ID: 1771419569687-->


В чем разница между `fprintf()` и `printf()`? #flashcard 
`printf()` выводит отформатированные строки только в стандартный поток вывода, а `fprintf()` позволяет выбрать поток вывода.
<!--ID: 1771419569690-->

Как открыть файл на запись? #flashcard 
Для этого нужна функция и флаги, которые определят в каком режиме и для чего файл будет открыт:
```C
#include <unistd.h>   // для функций `write()`, `open()`, `close()`.
#include <fcntl.h>    // для флагов.
#include <sys/stat.h> // для флагов.
int main() {
	int fd = open(<путь>, O_WRONLY | O_CREAT | O_TRUNC, S_IRWXU);
	int rc = write(fd, "hello world\n", 13);
	close(fd);
}
```
Флаг `O_WRONLY` открывает файл для чтения, `O_CREAT` создает файл, если он был не создан, `O_TRUNC` очищает файл, если он существовал. `S_IRWXU` задает правило для прав на созданный файл - они будут только у владельца файла (на чтение, запись и исполнение).
<!--ID: 1771518378864-->

Почему при записи в файл я говорю, что строка `"hello world\n"` занимает $13$ байт, а не $12$ (по числу символов)? #flashcard 
Это происходит, потому что в конце строки всегда пишется невидимый нулевой байт.
<!--ID: 1771518378869-->


Сколько байт весит символ переноса строки? #flashcard 
Один, несмотря на то, что он записывается с помощью двух символов.
<!--ID: 1771518378873-->

Что делает `fork()`? Какой заголовочный файл следует для нее использовать? #flashcard 
Создает копию текущего процесса, как отдельный процесс. Выделяет копии отдельное место в памяти, вносит туда регистры, выделяет там stack и heap. После новый процесс продолжит работу ровно с того места откуда была вызвана функция.
Заголовочный файл - `unistd.h`.
<!--ID: 1771931532848-->


Что возвращает `fork()`? #flashcard 
В родительском процессе она вернет PID нового дочернего процесса, а в дочернем процессе она вернет $0$. При возникновении ошибки в создании процесса вернется $-1$.
<!--ID: 1771931532851-->


Что делает `wait()`? Какой заголовочный файл следует для нее использовать? #flashcard 
Заставляет процесс ждать пока его дочерний процесс закончит работу.
Заголовочный файл - `wait.h`.
<!--ID: 1771931532854-->


Что делает функция `strdup()`? #flashcard 
`strdup` = *str*ing *dup*licate. Создает дубликат строки в heap-памяти. Сначала она измеряет длину строки, затем прибавляет байт для завершающего нуля и передает это значение в `malloc()`. Когда пространство готово, строка копируется в него и функция `strdup()` возвращает указатель на строку.
<!--ID: 1771931532857-->


Что делает `exec()`? #flashcard 
Эта функция принимает на вход путь до программы, список ее аргументов и затем стирает код текущей программы из памяти, чтобы загрузить в память и выполнить новую программу. `exec()` не возвращает никаких значений, потому что возвращать значение уже некуда.
<!--ID: 1771931532860-->

Что такое "мусорный адрес"? #flashcard 
Я встретил это понятие в контексте указателей. Когда мы инициализируем указатель:
```C
int *a;
```
Он уже указывает на какую-то случайную ячейку в памяти. Такая случайная ячейка и есть "мусорный адрес", т.к. она не может быть использована для записи значения в указатель - она просто является временной заглушкой.
<!--ID: 1772194374406-->

Какая библиотека поможет мне управлять файлами, например, открывать файлы? #flashcard 
`fcntl.h` (*f*ile *c*o*nt*ro*l*.h) - предоставляет функции `open()`, `close()`, `write()` и флаги для определения того, что можно сделать с файлом при открытии (например `O_WRONLY`).
<!--ID: 1772866022830-->

Если строка в памяти это просто последовательность байт, то как функции понимают где именно кончается строка? #flashcard 
Они понимают это благодаря *нуль-терминатору* - это специальный байт со значением `0`, который всегда ставится в конце строки.
<!--ID: 1773048695618-->


Как выглядит строка в памяти? #flashcard 
Строка в памяти - это последовательность ячеек памяти типа `char`.
<!--ID: 1773048695622-->

Чем отличается `long` от `long long`? #flashcard 
Это два типа данных, первый занимает от 4 байт до 8 байт в памяти (зависит от системы, на которой он используется). `long long` занимает ровно 8 байт в памяти, вне зависимости от системы.
<!--ID: 1774180411836-->


Как я могу вывести отформатированную строку (функция `printf()`) c числом типа `long`? #flashcard 
Для этого нужно использовать `%ld`:
```C
printf("%ld%", long_number);
```
<!--ID: 1774180411845-->


Как я могу получить текущее время самым простым способом? #flashcard 
```C
#include <sys/time.h>
...
stuct timeval now;
gettimeofday(&now, NULL);
printf("%ld", now.tv_sec);
...
```
Нужно понимать, что функция `gettimeofday` возвращает только код успешного или не успешного выполнения. Само время она записывает в ячейку памяти, на которую указывает переменная `now`.
Так же нужно знать, что это время в секундах от 1970 года. 
<!--ID: 1774180411850-->


До каких величин `gettimeofday()` может рассчитать время? #flashcard 
Секунды и микросекунды.
<!--ID: 1774180411855-->


Как я могу получить текущее время с наносекундами? #flashcard 
`clockgettime()` и структура `timespec` дают возможность использовать атрибут `ts_nsec`:
```C
#include <time.h>
...
struct timespec start;
clock_gettime(CLOCK_REALTIME, &start);
...
```
<!--ID: 1774180411859-->
