TARGET DECK: CSDeck

Что такое CRUD? #flashcard 
Это аббревиатура, которая описывает четыре базовые операции с данными в какой-нибудь системе - CREATE, READ, UPDATE, DELETE.
<!--ID: 1770103093236-->


Что такое MVC? #flashcard 
Это архитектура приложения. Model-View-Controller. Model - часть приложения выполняет всю бизнес-логику. View - это "лицо" приложения, то, что показывается пользователю. Controller - центр архитектуры; обрабатывает пользовательские запросы, которые получает View, отравляет что-то на обработку в Model, готовит ответ для пользователя и приказывает View показать этот ответ.
<!--ID: 1770103093239-->


Что такое MVP? #flashcard 
Это архитектура приложения. Model-View-Presenter. Model - часть приложения выполняет всю бизнес-логику. View - это "лицо" приложения, то, что показывается пользователю. Presenter - центр архитектуры, обрабатывает все, что происходит с View, отправляет что-то на обработку в Model, самостоятельно обновляет View, чтобы опубликовать ответ пользователю.
<!--ID: 1770103093242-->


Что такое MVVM? #flashcard 
Это архитектура приложения. Model-View-ModelView. Model - часть приложения выполняет всю бизнес-логику. View - это "лицо" приложения, то, что показывается пользователю. ModelView - то, что делает компоненты автономными в этой модели; она не управляет другими компонентами приложения, она имеет свои объекты внутри себя, обновляя которые дает сигнал Model и View, что нужно выполнить работу с обновленными объектами.
<!--ID: 1770103093245-->


Что такое CQRS? #flashcard 
Это архитектура приложения. Command-Query-Responsibility-Segregation. Это усовершенствование базового принципа работы с данными CRUD, которое основано на разделении операций записи-обновления-удаления данных и операций чтения данных на две разных модели.
<!--ID: 1770103093248-->


Что в архитектуре CQRS делает модель `Command` с данными? #flashcard 
Модель `Command` нужна для обновления данных.
<!--ID: 1770103093251-->


Что в архитектуре CQRS делает модель `Query` с данными? #flashcard 
Модель `Query` нужна, чтобы читать данные.
<!--ID: 1770103093253-->


Что такое DTO? #flashcard 
DTO = Data Transfer Object. Это объект, который переносит данные от одного компонента архитектуры к другому. Важно, что DTO - это комплект данных, на которые поступил запрос. Например, в CQRS часть приложения `Query` читает данные из БД и направляет на обработку пакетом в формате DTO.
<!--ID: 1770103093256-->


Что такое OOP? #flashcard 
OOP = Object Oriented Programming. Способ конструирования программы через классы и их объекты, где каждый класс репрезентирует какую-нибудь абстракцию, например, "программист", а каждый объект представляет собой конкретный экземпляр этой абстракции, например, "программист-Толя". каждый объект в такой системе имеет публичный интерфейс и приватное состояние, которое доступно только ему.
<!--ID: 1770103093259-->


Чтобы реализовать OOP-программу существует несколько основных принципов. Что это за принципы? #flashcard 
Абстракция - классы являются репрезентацией абстракций. Наследование - классы могут наследоваться друг от друга; классы-наследники сохраняют методы и атрибуты классов-родителей + имеют доступ к их приватным интерфейсам. Полиморфизм - связанные классы могут иметь схожие методы и предоставлять одинаковый интерфейс для них (например, два класса-потомка могут переопределять родительский метод, но метод все еще сохраняет свое имя). Инкапсуляция - классы могут иметь свои приватные атрибуты и приватные методы, которые не являются частью их публичного интерфейса.
<!--ID: 1770103093262-->

Как устроена память, которая выделяется программе операционной системой? #flashcard 
Такая память состоит из набора адресов. Это адреса ячеек памяти. Самые *маленькие* адреса - *низкие* адреса, а самые большие адреса - *высокие* адреса. Сверху вниз память хранит - переменные окружения + аргументы командной строки, STACK, место под расширение STACK-HEAP, HEAP, место для хранения неинициализированных статических и глобальных переменных, место для хранения инициализированных статических и глобальных переменных, а после всего этого лежит сам текст программы.
<!--ID: 1770531207475-->


Что хранит STACK? #flashcard 
Он хранит локальные переменные, параметры функций, адреса возврата (после вызова функции в стеке сохраняется адрес места куда нужно вернуться), некоторые регистры процессора (???).
<!--ID: 1770531207480-->

По какому принципу работает STACK? Если я записываю в эту область памяти что-то и что-то достаю, как происходит этот процесс? #flashcard 
stack работает по принципу FILO - first-in last-out. хорошая аналогия - стопка тарелок. я кладу первую тарелку, вторую, третью и если я захочу забрать тарелку, то получу первую сверху - третью снизу.
<!--ID: 1771156451608-->


Какой обычно размер у STACK? #flashcard 
В разных системах по разному, но по умолчанию обычно до 10МБ. Например, в UNIX системах 10МБ, в Windows - 1МБ.
<!--ID: 1771156451617-->


Что такое *stack-smashing*? #flashcard 
Это атака на ПО, суть которой заключается в переполнении STACK памяти программы мусорными данными. Вместе с мусорными данными передается новый адрес возврата из уязвимой функции - ячейка памяти программы со старым адресом возврата перезаписывается на новый и после завершения функции начинает выполняться совершенно другой код.
<!--ID: 1771156451620-->


Что такое ASLR? #flashcard 
ASLR = Address Space Layout Randomization. Это механизм, который защищает от *stack-smashing* атаки. С выключенной защитой адреса ячеек памяти программы всегда одинаковые, например, stack в любой программе начинается с адреса `0xbffff7c0`, но с включенной системой адреса перемешиваются - становится очень сложно понять куда именно следует записывать новый адрес возврата.
<!--ID: 1771156451623-->


Каков стандартный жизненный цикл процессора? #flashcard 
процессор всегда занят выполнением инструкций. он цепляет код инструкции из памяти, декодирует этот код и выполняет инструкцию. этот цикл иногда сокращенно называют *fetch-decode-execute*.
<!--ID: 1771156451626-->


За какой функционал в общем смысле отвечает ОС компьютера? #flashcard 
ОС выполняет механизм виртуализации. Множеству программ нужно выполняться одновременно - они должны использовать ресурсы физической памяти, процессора, дискового пространства. Для этого ОС виртуализует каждый из этих ресурсов, делая возможным их одновременное использование несколькими программами.
<!--ID: 1771156451629-->


Что такое системный вызов? #flashcard 
Системные вызовы - это часть операционной системы компьютера, которая позволяет программам отправлять запросы на выполнение привилегированных команд в ядро компьютера. Это интерфейс, который реализует ОС для общения программ и ядра.
<!--ID: 1771156451632-->

